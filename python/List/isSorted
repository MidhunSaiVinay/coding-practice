def isSorted(arr):
    i=0
    if len(arr)==0 or len(arr)==1:
        return True
    while i<len(arr)-1:
        if arr[i]<arr[i+1]:
            i+=1
        else:
            return False
    return True
arr=eval(input())
print(isSorted(arr))


